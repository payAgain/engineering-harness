# Optional Codex adapter

## Recommended usage

- Provide `PROTOCOL.md` as the task specification
- Codex should treat `agents/*.md` as role prompts and `skills/*.md` as procedures
- Persist state only into the target repository harness files

No global Codex skill installation is required.
